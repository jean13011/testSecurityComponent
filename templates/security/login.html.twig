{% extends 'base.html.twig' %}

{% block title %}
	connexion
{% endblock %}

{% block body %}
	<h1>connexion</h1>

	<a class="nav-link" href="{{ path('security_registration') }}">inscription</a>

	<form action=" {{ path('security_login') }} " method="post">
		<div class="form-group">
			<input placeholder="adresse mail" type="text" required name='_username' class="form-control">
		</div>
		<div class="form-group">
			<input placeholder="mot de passe" type="password" required name='_password' class="form-control">
		</div>
		<div class="form-group">
			<button type="submit" class="btn btn-succes">Connexion</button>
		</div>
	</form>
{% endblock %}
